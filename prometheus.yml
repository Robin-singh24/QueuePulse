global:
  scrape_interval: 5s

scrape_configs:

  - job_name: "pulsequeue-api"

    static_configs:
      - targets: ["api:8000"]

  - job_name: "pulsequeue-worker"

    static_configs:
      - targets: ["worker:9000"]